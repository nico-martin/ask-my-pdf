import Model from './Model.ts';

const model = new Model({
  title: 'Chrome built-in Prompt API (experimental)',
  url: null,
  size: 0,
  libUrl: null,
  requiredFeatures: null,
  cardLink: 'https://github.com/explainers-by-googlers/prompt-api',
  about: null,
  id: 'prompt-api',
});

export default model;
